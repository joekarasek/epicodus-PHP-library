<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
  </head>
  <body>
    {% include '/includes/patron-navbar.html.twig' %}
    <div class="container">
      {% include '/includes/message.html.twig' %}
      <h2>Book Title: {{ book.getTitle }}</h2>
      <h3>Written By:
          {% for author in book.getAuthors %}
            {{ author.getName }}
          {% endfor%}
      </h3>
      <h3>Number of copies in catalog: {{ book.countCopies }}</h3>
      <h3>Number of copies available: {{ book.countCopiesAvailable }}</h3>
      {% if book.countCopiesAvailable != 0 %}
          <form action="/patron/{{ patron.getId }}/book/{{ book.getId }}/checkout" method="post">
            <button type='submit' class='btn btn-danger'>Checkout this book!</button>
          </form>
      {% endif %}
    </div>
  </body>
</html>
